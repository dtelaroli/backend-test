const cart = require("./cart");
const product = require("./product");
const sku = require("./sku");
const query = require("./query");
const mutation = require("./mutation");

module.exports = [cart, product, sku, query, mutation];
